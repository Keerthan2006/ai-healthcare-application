apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: healthcare

spec:
  type: ClusterIP

  selector:
    app: api-deploy

  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP