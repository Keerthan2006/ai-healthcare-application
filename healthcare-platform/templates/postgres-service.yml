apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: healthcare
spec:
  type: ClusterIP
  selector:
    app: postgres-state
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
