apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: healthcare

spec:
  type: ClusterIP

  selector:
    app: ai-service

  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP