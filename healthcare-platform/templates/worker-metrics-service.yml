apiVersion: v1
kind: Service

metadata:
  name: worker-metrics
  namespace: healthcare

spec:
  type: ClusterIP

  selector:
    app: worker

  ports:
    - name: metrics
      port: 8003
      targetPort: 8003
      protocol: TCP